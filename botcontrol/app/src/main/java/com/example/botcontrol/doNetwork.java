package com.example.botcontrol;

public class doNetwork implements Runnable{
    //probably needs to have public variables for pieces from main that I need in this class
    //in the main class, probably need *new doNetwork();* and *doNetwork.variable = variable;*
    //syntax might not be perfect but the idea seems to make sense
    //needed variables: IP and Players (i think)
    @Override
    public void run() {
        //question of the day: should it be it's own file?
        //I expect this to be rather similar to Jim's code
    }
}
